// noStroke();

let draw = function() {
    stroke(10);
    fill(mouseX, mouseY, 0);
    ellipse(mouseX, mouseY, 28, 10);
};

// either work???

function draw2() {
    stroke(10);
    fill(mouseX, mouseY, 0);
    ellipse(mouseX, mouseY, 28, 10);
}
